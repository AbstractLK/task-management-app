# Task Management Task Service

Node.js and Express microservice for task CRUD operations. Each task stores a `userId` from the authenticated JWT.

## Endpoints

- `GET /health`
- `GET /`
- `POST /`
- `PUT /:id`
- `DELETE /:id`
