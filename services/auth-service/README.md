# Task Management Auth Service

Node.js and Express microservice for user registration, login, JWT generation, and token validation.

## Endpoints

- `GET /health`
- `POST /register`
- `POST /login`
- `GET /me`
